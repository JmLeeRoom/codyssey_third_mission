# 5. 평가문항 (출제 의도 및 발표 답안)

---

## 항목 1: 기능 동작 검증

- [x] **모드 1에서 3×3 필터 2개와 3×3 패턴 입력 후, MAC 점수/판정(A/B/판정 불가)/시간(ms)이 출력되는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: Mini NPU 시뮬레이터의 가장 기본적이고 핵심적인 단위 기능(Unit Integration)을 검증하기 위함입니다. 사용자가 직접 3×3 행렬을 입력할 수 있는 CLI 인터페이스, MAC 점수 계산 커널, 판정 로직, 그리고 지연시간 측정 모듈이 하나로 상호작용하여 올바른 형식으로 결과를 출력하는지 파이프라인의 입출력 정상 작동 여부를 평가합니다.
  - 📝 **[완벽 답안 및 발표 내용]**: `main.py`의 `run_mode_1()` 실행 시 필터 A, B 및 패턴 3×3 행렬을 콘솔 입력으로 받아 `mac()` 함수로 각 필터와의 내적 점수를 산출합니다. 그 후 `judge_scores()`가 판정(A/B/판정 불가)을 내리며, `measure_mac_time()`을 통해 10회 평균 연산 지연 시간(ms 단위, 소수점 4자리)을 콘솔에 정확히 출력하도록 구현했습니다.

- [x] **모드 1에서 허용오차 내 동점이 발생했을 때 “판정 불가”가 출력되는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 부동소수점 연산에서 발생할 수 있는 경계 조건(Edge Case) 처리 능력을 평가하기 위함입니다. 두 필터의 점수 차이가 미세할 때 특정 필터로 잘못 분류되는 비결정적 오탐(False Decision)을 방지하고, 사용자에게 명확하고 직관적인 한국어 안내 메시지를 제공하여 경계 예외 처리의 완성도를 검증합니다.
  - 📝 **[완벽 답안 및 발표 내용]**: `judge_scores()` 함수에서 두 점수의 절대 차이인 `abs(score_left - score_right) < EPSILON(1e-9)` 조건을 만족하면 `"UNDECIDED"`를 반환합니다. 대화형 모드 1 콘솔에서는 이를 `판정: 판정 불가 (|A-B| < 1e-09, epsilon 동점 규칙)` 양식의 직관적인 사용자 안내문으로 변환하여 출력합니다.

- [x] **모드 1 입력에서 행/열 개수 불일치 또는 숫자 파싱 실패 시, 오류 안내 후 재입력을 유도하는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: CLI 프로그램의 방어적 프로그래밍(Defensive Programming) 및 예외 처리(Exception Handling) 능력을 검증하기 위함입니다. 사용자가 문자를 입력하거나 공백 구분을 잘못 입력했을 때 프로그램이 비정상 종료(Crash)되지 않고 안정적으로 오류 안내 후 입력을 재시도하게 만드는 견고성(Robustness)을 평가합니다.
  - 📝 **[완벽 답안 및 발표 내용]**: `read_grid_3x3()` 함수가 `input()`으로 입력을 받을 때 줄당 숫자 개수가 3개가 아니거나 `float()` 변환 예외(문자/특수문자 입력 등)가 발생하면 오류를 포착합니다. 추가로 `validate_grid()`로 정방 행렬 크기 및 숫자 유효성을 사전 검증하여 failure 시 원인 메시지(`입력 형식 오류: ...`)를 안내한 뒤 `while True` 루프를 통해 해당 행렬 전체를 다시 입력받도록 설계했습니다.

- [x] **모드 2에서 data.json의 filters/patterns를 로드하고, 패턴 키(size_{N}_{idx})에서 N을 추출해 올바른 필터를 선택하는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 파일 기반 배치(Batch) 분석에서 데이터 규약(Schema) 파싱 및 동적 필터 매칭 능력을 평가하기 위함입니다. 다양한 패턴 크기($N=5, 13, 25$ 등)에 대해 하드코딩 없이 키 정규표현식 파싱을 통해 대응하는 필터 룩업 테이블을 정밀하게 매칭시키는 유연한 데이터 파이프라인 구조를 검증합니다.
  - 📝 **[완벽 답안 및 발표 내용]**: `CASE_KEY_RE = re.compile(r'^size_(\d+)_(.+)$')` 정규식을 활용하여 패턴 키(예: `size_5_1`, `size_13_2`)의 첫 번째 캡처 그룹에서 정수 $N$을 추출합니다. 이 $N$을 Key로 `filters_by_size[N]` 사전에서 상응하는 `Cross` 및 `X` 필터를 $O(1)$로 탐색하여 MAC 연산을 진행하도록 구현했습니다.

- [x] **expected(‘+’, ‘x’)와 filter 키(‘cross’, ‘x’)가 프로그램 내부에서 표준 라벨(Cross/X)로 정규화되어 비교되는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 실무 데이터셋에서 빈번히 발생하는 라벨 불일치(Data Noise 및 표기법 다변화) 문제를 원천 차단하는 도메인 모델 표준화(Normalization) 능력을 평가하기 위함입니다. 대소문자나 표기 기호가 달라도 내부적으로 일관된 라벨로 매핑하여 판정 및 대조 오탐을 차단하는 도메인 설계 감각을 검증합니다.
  - 📝 **[완벽 답안 및 발표 내용]**: `normalize_label()` 함수를 통해 원본 문자열의 공백 제거 및 소문자 변환 후 `+`, `cross` -> `"Cross"`, `x` -> `"X"`로 일괄 변환합니다. `validate_filter_set()`과 `analyze_case()` 전 과정에서 필터 키 저장, 판정, expected 대조 시 오직 정규화된 표준 라벨(`Cross`, `X`)만을 사용하여 라벨 불일치 버그를 차단했습니다.

- [x] **모드 2에서 허용오차 내 동점이 발생했을 때 “UNDECIDED”로 출력되고 FAIL로 집계되는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 자동화된 테스트 검증 시스템(Automated Grading/Testing System)에서 펠세이프(Fail-Safe) 평가 엄격성을 검증하기 위함입니다. 동점으로 분류된 미결정 상태를 모호하게 통과시키지 않고 엄격한 품질 게이트(Quality Gate)에 따라 명확히 `FAIL`로 처리하여 채점 시스템의 정확도를 유지하는지 평가합니다.
  - 📝 **[완벽 답안 및 발표 내용]**: 모드 2 분석 중 `judge_scores()`가 `"UNDECIDED"`를 반환하면 콘솔에 `판정: UNDECIDED | expected: ... | FAIL`로 출력되며 사유 코드로 `UNDECIDED`를 기록합니다. expected 라벨이 정규화된 라벨(`Cross` 또는 `X`)인 상황에서 판정이 `"UNDECIDED"`이면 expected와 일치하지 않으므로 무조건 `FAIL`로 집계됩니다 (예: 번들 데이터의 `size_13_1` 케이스).

- [x] **성능 분석 표에 크기, 평균 시간(ms), 연산 횟수(N²)가 출력되는가(크기별 10회 평균)?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 단순 기능 구현을 넘어 성능 벤치마크 지표(Performance Metrics)를 정량화(Quantify)하고 시각화하는 소프트웨어 벤치마킹 능력을 평가하기 위함입니다. 지연시간과 이론적 연산 횟수를 비교표 양식으로 정렬하여 제출하는 엔지니어링 보고 능력을 검증합니다.
  - 📝 **[완벽 답안 및 발표 내용]**: `print_performance_table()` 및 `build_performance_rows()` 함수가 3×3, 5×5, 13×13, 25×25 크기별로 `PERF_REPEAT=10`회 연산 지연시간을 측정합니다. 콘솔 출력 표에는 크기($N \times N$), 10회 평균 시간(ms, 소수점 4자리), 그리고 1회 MAC 연산 기준 $N^2$ 연산 횟수(9, 25, 169, 625)를 정돈된 칼럼 양식으로 출력합니다.

- [x] **콘솔에서 총 테스트/통과/실패 및 실패 케이스 요약이 출력되는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: CI/CD 테스트 러너(Test Runner) 패러다임을 콘솔 애플리케이션에 적용할 수 있는지 평가하기 위함입니다. 배치 작업 완료 후 실행 요약 통계와 실패 케이스의 원인을 빠르게 파악할 수 있는 개발자 중심의 CLI UX 구현 능력을 검증합니다.
  - 📝 **[완벽 답안 및 발표 내용]**: `print_summary()` 함수에서 전체 분석 케이스 수(`Total`), PASS 건수, FAIL 건수를 집계 및 출력합니다. FAIL이 존재할 경우 하단 섹션에 실패 케이스 ID 및 원인 메시지(`size_13_1: 동점...`, `size_13_3: 크기 불일치...`)를 리스트 형태로 요약 제시합니다.

- [x] **README에 “결과 리포트(실패 원인 + 복잡도)” 섹션이 존재하는가(최소 10줄)?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 개발자의 핵심 역량인 기술 문서화(Technical Documentation) 및 데이터 기반 분석 자질을 평가하기 위함입니다. 코드 작성에 그치지 않고 실패 원인을 데이터/수치/로직별로 분류하고, 알고리즘의 복잡도를 문서로 입증할 수 있는지 검증합니다.
  - 📝 **[완벽 답안 및 발표 내용]**: `README.md`의 `5. 결과 리포트` 섹션에 60줄 이상의 상세한 분석이 포함되어 있습니다. 동점 수치 실패(`size_13_1`)와 스키마 불일치 실패(`size_13_3`)의 사유 분류, $O(N^2)$ 시간 복잡도의 수렴 경향성, 인터프리터 고정 오버헤드 영향, 2D vs 1D 평탄화 비교가 완벽히 기술되어 있습니다.

---

## 항목 2: 코드 구조 및 설계

- [x] **MAC 연산 로직의 전체 흐름(입력 → 위치별 곱셈 → 누적 합산 → 점수 반환)을 코드 구조 기준으로 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 코드를 단순히 따라 쓴 것이 아니라 알고리즘의 데이터 흐름(Data Flow)과 제어 구조(Control Flow)를 완벽히 이해하고 대면 인터뷰에서 설명할 수 있는지 소프트웨어 기본 역량을 평가하기 위함입니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    1. **입력 검증**: `validate_grid()`에서 2차원 리스트의 정방형($N \times N$) 구조, 숫자 형태, NaN/Inf 검증이 완료된 `Grid` 객체를 전달받습니다.
    2. **크기 검사**: `mac(pattern, filter_grid)` 진입 시 `len(pattern) != len(filter_grid)`인 경우 즉시 `ValueError` 예외를 발생시켜 비정상 연산을 예방합니다.
    3. **위치별 곱셈 및 누적**: 외부 라이브러리 없이 이중 `for` 루프(행 `r`, 열 `c`)를 순회하며 `total += pattern[r][c] * filter_grid[r][c]`로 원소별 곱을 누적 합산합니다.
    4. **점수 반환**: 최종 누적된 float 형태의 `total` 내적 값을 반환합니다.

- [x] **patterns 키에서 크기 N을 추출하고, filters의 size_N과 매칭하는 로직을 어떻게 설계했는지 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 정규표현식을 활용한 스트링 파싱 능력과 해시 테이블(Dict) 룩업을 통한 효율적인 $O(1)$ 매칭 알고리즘 설계 능력을 검증하기 위함입니다. 또한 키 불일치 시 전체 실행을 중단시키지 않는 실패 격리(Failure Isolation) 구조 설계를 평가합니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    1. 정규식 `r'^size_(\d+)_(.+)$'`를 이용하여 패턴 키(예: `size_5_1`)에서 캡처 그룹 `(\d+)`을 통해 정수 $N$을 추출합니다.
    2. 사전 로딩 시 `filters` 키 `size_N`을 `FILTER_KEY_RE`로 정규화해 `filters_by_size[N] = {'Cross': grid1, 'X': grid2}` 구조로 보관합니다.
    3. 추출한 $N$으로 `filters_by_size.get(N)`을 조회하여 $O(1)$의 빠르고 안정적인 필터 매칭을 수행하며, 키 형식 오류나 필터 부재 시 독립 사유코드(`INVALID_CASE_KEY`, `FILTER_NOT_FOUND`)로 감싸 개별 FAIL로 격리 처리합니다.

- [x] **라벨 정규화(normalize_label)를 별도 함수/모듈로 분리한 이유와 장점을 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 소프트웨어 공학의 핵심 원칙인 단일 책임 원칙(SRP)과 낮은 결합도(Loose Coupling) 개념의 실천 여부를 검증하기 위함입니다. 정규화 로직을 분리함으로써 얻는 유지보수성 및 안정성을 논리적으로 설명할 수 있는지 평가합니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    - **분리 이유**: JSON 데이터 내 기호/문자열 표기 차이(`+`, `cross`, `Cross`, `x`, `X`)를 단일 관점 함수에서 전처리하여 메인 하드코딩 오염을 방지하기 위함입니다.
    - **장점**:
      1. **단일 책임 원칙(SRP)**: 정규화 매핑 규칙 변경 시 `normalize_label()` 한 곳만 수정하면 됩니다.
      2. **예외 처리 단일화**: 대소문자 통일 및 공백 제거(`strip().lower()`)를 일괄 적용하고, 미정의 라벨은 `None`을 반환해 검증을 명확히 합니다.
      3. **비교 안정성**: 점수 계산 및 집계 로직 전체에서 정규화된 `"Cross"`, `"X"` 상수로만 대조하여 대소문자나 오탈자로 인한 버그를 차단합니다.

- [x] **시간 측정 시 I/O를 배제하고 “연산 구간”을 측정하기 위해 어떤 경계를 잡았는지 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 컴퓨터 시스템 벤치마킹에서 "순수 연산 시간(Pure Compute Time)"과 "I/O 병목 노이즈"를 엄격히 분리하는 측정 실험 설계 능력을 평가하기 위함입니다. 측정 경계(Profiling Boundary)의 설정 타당성을 파악하는지 검증합니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    - `measure_mac_time()` 함수 내부에서 `time.perf_counter()`의 호출 시점을 연산 구간의 정확한 경계로 설정했습니다.
    - JSON 파싱, 파일 I/O, 콘솔 출력(`print`), 입력 검증 과정은 측정 구간 **외부**에 배치하고, 오직 순수 `mac(pattern, filter_grid)` 지연시간 측정 루프 바로 직전(`start = time.perf_counter()`)과 직후(`elapsed = ...`)만을 측정 경계로 감싸 pure execution time(ms)을 계산했습니다.

---

## 항목 3: 핵심 기술 원리 적용

- [x] **MAC 점수가 높을수록 “더 유사하다”고 판단할 수 있는 이유를 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: AI 및 NPU 아키텍처의 수학적 근간인 선형대수학(Linear Algebra)의 내적(Dot Product) 개념과 공간 상관관계(Spatial Correlation) 원리를 알고리즘과 연계하여 설명할 수 있는지 심층 이론 지식을 평가하기 위함입니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    - MAC 연산 $\sum (P_{i,j} \times F_{i,j})$은 수학적으로 두 2차원 행렬(벡터)의 **내적(Dot Product)**과 일치합니다.
    - 양의 값을 가질 때, 패턴의 픽셀 위치와 필터의 픽셀 위치가 많이 겹칠수록 원소 곱이 큰 양수가 되어 내적 누적합이 최대화됩니다.
    - 따라서 내적 값이 크다는 것은 패턴과 필터의 공간적 형상(Spatial Layout) 상관관계(Correlation)가 매우 높음을 의미하므로 MAC 점수가 높을수록 두 패턴이 더 유사하다고 판정할 수 있습니다.

- [x] **패턴 크기가 N일 때 연산 횟수가 N²로 증가하며, 측정 결과가 O(N²) 경향을 보이는 이유를 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 알고리즘 시간 복잡도(Big-O Notation) 이론과 실측 실행 시간 간의 연관성을 증명할 수 있는지 평가하기 위함입니다. N이 커짐에 따라 이론적 연산 횟수 $N^2$과 실행 시간이 비례 수렴하는 원리 및 소형 N에서의 오버헤드 특성을 진단할 수 있는지 검증합니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    - $N \times N$ 크기 행렬의 MAC 연산은 $N^2$개 전체 원소에 대해 각각 곱셈 1회와 덧셈 1회를 수행하므로 연산 횟수가 정확히 $N^2$입니다.
    - 실측 데이터 검증: $13 \times 13 \to 25 \times 25$ 구간에서 연산 횟수는 169회 $\to$ 625회로 약 $3.70$배 증가하였고, 측정된 지연시간 역시 $0.0077\text{ ms} \to 0.0246\text{ ms}$로 약 $3.19$배 증가하여 이론적인 $O(N^2)$ 수렴 경향을 보여줍니다.
    - 소형 크기에서는 파이썬 루프 제어 오버헤드가 일정 비중을 차지하나, $N$이 커질수록 $N^2$ 복잡도 비용이 전체 지연시간을 지배하게 됩니다.

- [x] **부동소수점 오차가 왜 발생하며, 비교 정책(epsilon)이 왜 필요한지 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 컴퓨터 시스템의 수치 표현(IEEE 754 2진 부동소수점 한계) 문제와 수치 해석학(Numerical Analysis)에서의 오차 제어 감각을 평가하기 위함입니다. float의 단순 비교 위험성을 파악하고 epsilon 비교 정책을 통한 수치적 안정성(Numerical Stability) 확보 능력을 평가합니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    - **발생 원인**: 컴퓨터는 2진 부동소수점(IEEE 754)을 사용하므로, 10진 소수 연산 시 무한소수로 인한 미세한 반올림 오차가 덧셈 누적 과정에서 발생합니다.
    - **필요성**: float 값을 단순 동등 비교(`score_a == score_b`)할 경우 이론적으로 같아야 할 값들이 부동소수점 미세 오차 때문에 서로 다른 값으로 판단되는 문제가 생깁니다.
    - **비교 정책**: 따라서 두 점수의 차이 `abs(score_a - score_b)`가 임계 오차 `EPSILON(1e-9)` 미만인 경우 동점(`UNDECIDED`)으로 처리하는 오차 허용 비교 정책을 적용하여 연산 유효성과 수치적 안정성을 보장합니다.

- [x] **동점 처리 규칙을 “mode1 vs mode2”로 다르게 둔 이유를 요구사항 관점에서 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 동일한 기술적 이슈(동점)라도 대화형 UX(사용자 직관성)와 자동화 CI(품질 엄격성)라는 서비스/시스템 요구사항의 목적에 따라 설계 선택이 달라질 수 있음을 이해하는지 평가하기 위함입니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    - **모드 1(대화형 사용자 입력)**: 일반 사용자 콘솔 경험(UX) 측면에서 직관적이고 친숙한 텍스트 표현인 `"판정 불가"` 형태로 결과를 안내하도록 설계되었습니다.
    - **모드 2(data.json 배치 검증)**: 명세된 정규화 스키마 계약 및 테스트 자동화 관점에서 `"UNDECIDED"` 상수로 반환합니다. 기대 라벨(`Cross`/`X`)이 지정된 상태에서 동점이 나온 상황은 모호한 미결정 상태이므로 테스트 검증 엄격성을 유지하기 위해 `FAIL`로 명확하게 집계/격리합니다.

---

## 항목 4: 심층 인터뷰

- [x] **만약 data.json에 새로운 라벨(예: ‘o’)이 추가된다면, 정규화/판정/출력/테스트를 어떤 순서로 확장할지 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 소프트웨어의 개방-폐쇄 원칙(OCP)과 확장성(Scalability) 관점에서 새로운 기능 요구사항이 들어왔을 때 기존 코드를 파괴하지 않고 단계별로 정밀 확장하는 엔지니어링 프로세스 능력을 검증하기 위함입니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    1. **정규화 확장**: `normalize_label()`에 `'o'`, `'circle'`, `'Circle'`을 표준 라벨 `"Circle"`로 매핑하는 케이스를 추가합니다.
    2. **필터 검증 확장**: `validate_filter_set()`의 필수 필터 검사 목록을 `["Cross", "X", "Circle"]`로 확대합니다.
    3. **판정 알고리즘 범용화**: `judge_scores()`를 $M$개 라벨 중 최댓값을 찾고 1위와 2위의 차이가 `EPSILON` 미만인 경우 `"UNDECIDED"`를 반환하도록 N-Way 비교 알고리즘으로 범용화합니다.
    4. **출력/스키마 업데이트**: 모드 2 로그 및 성능 리포트에 Circle 점수 항목을 추가합니다.
    5. **단위 테스트 추가**: `tests/test_main.py`에 Circle 라벨 정규화, 3-Way 분류, 동점 처리 및 회귀 테스트 케이스를 반영합니다.

- [x] **1000×1000 패턴을 처리한다면 시간/메모리 측면에서 어떤 문제가 발생하며, 어떤 최적화를 우선 적용할지 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 데이터 스케일(Scale-Up)에 따른 성능 병목(파이썬 객체 오버헤드, 캐시 미스, $O(N^2)$ 시간 지연)을 이해하고, 하드웨어 친화적(Hardware-aware) 최적화 전략(1D 연속 버퍼, SIMD, 멀티코어)을 제시할 수 있는지 평가하기 위함입니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    - **발생 문제**: $1000 \times 1000$은 원소 수가 $1,000,000$개($N^2=10^6$)입니다. 파이썬 중첩 리스트(`List[List[float]]`) 사용 시 객체 포인터 오버헤드로 메모리 낭비가 발생하며, 순수 파이썬 이중 `for` 루프 연산 속도가 극도로 저하됩니다.
    - **우선 최적화 방안**:
      1. **메모리 평탄화(1D Array Buffer)**: 본 프로젝트 보너스 구현(`flatten()`)처럼 연속된 1차원 메모리 버퍼로 변환하여 2D 인덱스 참조 오버헤드를 줄입니다.
      2. **C-extension / SIMD 벡터화**: NumPy 또는 C/C++ 바인딩을 통해 CPU SIMD(AVX2/AVX-512) 명령어 세트로 연산을 병렬화합니다.
      3. **청킹(Chunking) 및 멀티스레딩**: 행렬을 블록으로 분할하여 멀티코어 병렬 처리(OpenMP/Multiprocessing)를 도입합니다.

- [x] **CPU의 직렬 처리와 NPU의 병렬 처리 관점에서, 이 문제(대규모 MAC)의 병목이 어디에 생기는지 설명할 수 있는가?**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 딥러닝 가속기(NPU) 구조와 범용 프로세서(CPU)의 명령어 집합 및 메모리 계층 차이를 이해하고 있는지 아키텍처 관점의 심층 역량을 평가하기 위함입니다. Compute-bound 대 Memory-bound 병목 전환 개념을 검증합니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    - **CPU 관점**: CPU는 시퀀셜(Sequential) 명령 실행과 복잡한 제어 흐름/캐시 관리에 최적화되어 있으므로, 대규모 MAC 실행 시 ALU 스루풋 한계와 루프 제어/메모리 인덱싱 오버헤드가 주요 **연산 병목(Compute Bottleneck)**이 됩니다.
    - **NPU 관점**: NPU는 Systolic Array 구조를 통해 수천 개의 MAC 연산기를 고도로 병렬화하여 $1000 \times 1000$ 연산 지연시간을 획기적으로 줄입니다. 그러나 메인 DRAM에서 NPU On-chip SRAM/PE 버퍼로 데이터를 전송하는 **메모리 대역폭(Memory Bandwidth & I/O Bottleneck)**이 주 병목이 됩니다.

---

## 항목 5: 보너스 문제 해결에 따른 크레딧 부여

- [x] **보너스 문제 해결에 따른 크레딧 부여 (패턴 생성기 및 1D/2D 메모리 구조 비교)**
  - 💡 **[코디세이 출제 의도 및 당위성]**: 단순 기본 요구사항 만족(MVP)에 만족하지 않고, 능동적으로 임의 크기 패턴 자동 생성기 및 1차원 연속 메모리 구조(C-contiguous Memory)에 따른 성능 이점을 실험적으로 증명해낸 깊이 있는 탐구 엔지니어링 자질을 평가하여 우대 크레딧을 부여하기 위함입니다.
  - 📝 **[완벽 답안 및 발표 내용]**:
    1. `generate_cross_pattern(n)` 및 `generate_x_pattern(n)` 함수로 임의 크기 $N \times N$ 패턴 자동 생성기를 구현하였습니다.
    2. 2차원 리스트와 1차원 평탄화(`flatten()`) 메모리 구조 간 MAC 연산 성능을 동일 입력·동일 반복 조건으로 측정 및 비교(`build_flatten_comparison_rows()`)하여 메모리 인덱싱 오버헤드 절감 및 $O(N^2)$ 임계점 특성을 실험적으로 증명하여 요구사항을 초과 달성하였습니다.
